using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ObjectiveSystem : MonoBehaviour
{
    [SerializeField]
    GameObject area1, area2, area3, area4, area5, area6, area7;

    private void Update()
    {
        if(area1.transform.childCount < 1)
        {

        }
    }
}
